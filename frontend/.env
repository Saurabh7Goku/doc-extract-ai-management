# Next.js Environment Variables
# Variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# Copy this file to .env and fill in your actual values

# Backend API URL (browser / local dev)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Internal URL used when the app runs inside Docker (can reach backend service name)
BACKEND_INTERNAL_URL=http://backend:8000

# WebSocket URL (if different from API URL)
NEXT_PUBLIC_WS_URL=ws://localhost:8000

# Note: ADMIN_SECRET_KEY is a backend environment variable only
# It should be set in the backend/.env file, not here
